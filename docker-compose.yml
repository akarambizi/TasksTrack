version: '3.8'
services:
    # server:
    #   build: ./server
    #   ports:
    #     - 5000:5000
    #   volumes:
    #     - ./server:/app
    #   environment:
    #     - ASPNETCORE_ENVIRONMENT=Development

    client:
        build: ./client
        ports:
            - 5173:5173
        volumes:
            - ./client:/app
            - /app/client/node_modules
        command: yarn dev
        # depends_on:
        #   - server
